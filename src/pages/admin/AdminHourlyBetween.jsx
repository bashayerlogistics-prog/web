import AdminFleetServicePage from '../../components/admin/AdminFleetServicePage';

/** Hourly packages between cities (excludes within-city / internal). */
export default function AdminHourlyBetween() {
  return <AdminFleetServicePage serviceId="hourly" />;
}
